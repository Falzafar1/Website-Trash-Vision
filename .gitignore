# Dependencies Node.js
Backend/node_modules/

# File database SQLite lokal (tidak perlu di-upload, Turso yang menyimpan datanya)
Backend/riwayat_sistem.db

# Foto-foto dari Raspberry Pi (tidak perlu di-upload, dikirim langsung ke server)
Backend/uploads/*
# Tapi tetap sertakan folder uploads/ agar Railway bisa membuatnya
!Backend/uploads/.gitkeep

# OS files
.DS_Store
Thumbs.db

# Environment variables — JANGAN di-commit, ini berisi token rahasia!
.env
Backend/.env
